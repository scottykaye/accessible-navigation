{
  "name": "root",
  "workspaces": [
    "docs",
    "sandbox",
    "packages/*"
  ],
  "private": true,
  "devDependencies": {
    "turbo": "^1.12.4"
  },
  "scripts": {
    "build": "turbo run build"
  }
}
